name: Cron Test

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        run: |
          echo "Hello from GitHub Actions!"
          echo "Triggered at: $(date -u)"
